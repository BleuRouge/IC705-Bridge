[project]
name = "ic705bridge-demo"
version = "0.1.0"
description = "Environnement reproductible pour la démonstration IC705 Bridge"
requires-python = ">=3.10"
dependencies = [
    "ic705bridge",
    "matplotlib>=3.8",
    "numpy>=1.24",
]

[tool.uv]
package = false

[tool.uv.sources]
ic705bridge = { path = "../python", editable = true }
